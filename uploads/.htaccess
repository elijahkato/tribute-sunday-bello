# Prevent any script from ever executing inside the uploads tree, even if
# someone manages to get a disguised .php file past the upload validation.
<FilesMatch "\.(php|phtml|php\d|phar|cgi|pl)$">
    Require all denied
</FilesMatch>
Options -ExecCGI
php_flag engine off
